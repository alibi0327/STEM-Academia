STEM Academia — Arduino FULL v3 VISUAL

Добавлено:
- большой визуальный постер курса;
- схема элементов Arduino Uno;
- схема рабочего процесса USB → Board/Port → Verify → Upload;
- визуализация setup()/loop();
- схема breadboard;
- концептуальная схема LED + резистор;
- схема вход → Arduino → выход;
- Serial Monitor;
- изображения встроены прямо в соответствующие уроки.

База данных НЕ меняется. SQL запускать НЕ НУЖНО.
Нужно только заменить файлы, Commit, Push origin и Ctrl+F5.
