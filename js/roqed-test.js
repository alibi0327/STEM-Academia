import {supabase} from "./supabase.js";import{requireRole,$,esc}from"./utils.js";import{questions}from"./roqed-test-data.js";let session,course,last,current=0,sel=Array(questions.length).fill(null);
async function init(){let a=await requireRole("teacher");if(!a)return;session=a.session;let r=await supabase.from("courses").select("id").eq("slug","roqed-science").single();course=r.data;r=await supabase.from("lessons").select("id").eq("course_id",course.id).order("lesson_order",{ascending:false}).limit(1);last=r.data?.[0];render()}
function render(){let q=questions[current];$("counter").textContent=`Вопрос ${current+1} из ${questions.length}`;$("testProgress").style.width=(current+1)/questions.length*100+"%";$("questionCard").innerHTML=`<h2>${esc(q.question)}</h2><div class="answers">${q.answers.map((a,i)=>`<button class="answer ${sel[current]===i?"selected":""}" data-i="${i}">${esc(a)}</button>`).join("")}</div><div class="form-actions"><button id="prev" class="btn secondary" ${current===0?"disabled":""}>← Назад</button><button id="next" class="btn" ${sel[current]===null?"disabled":""}>${current===questions.length-1?"Завершить тест":"Далее →"}</button></div>`;document.querySelectorAll(".answer").forEach(b=>b.onclick=()=>{sel[current]=+b.dataset.i;render()});$("prev").onclick=()=>{current--;render()};$("next").onclick=()=>current<questions.length-1?(current++,render()):finish()}
async function finish(){let correct=questions.reduce((n,q,i)=>n+(sel[i]===q.correct),0),score=Math.round(correct/questions.length*100),passed=score>=80;let r=await supabase.from("test_results").insert({teacher_id:session.user.id,lesson_id:last.id,score,answers:{selected:sel,passed}});if(r.error)return alert(r.error.message);let cert=null;if(passed){r=await supabase.rpc("issue_course_certificate",{p_course_slug:"roqed-science"});if(!r.error)cert=r.data}$("questionCard").classList.add("hidden");$("resultBox").classList.remove("hidden");$("resultBox").innerHTML=`<h2>${passed?"Тест пройден":"Тест не пройден"}</h2><div class="score">${score}%</div><p>${correct} из ${questions.length} правильных ответов</p>${passed&&cert?`<a class="btn success" href="certificate.html?id=${encodeURIComponent(cert)}">Открыть сертификат</a>`:'<a class="btn" href="roqed-test.html">Пройти повторно</a>'}`};init().catch(e=>alert(e.message));