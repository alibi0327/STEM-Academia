import{supabase}from'./supabase.js';import{requireRole,logout,$,esc}from'./utils.js';async function init(){const a=await requireRole(['admin','school_admin']);if(!a)return;$('logout').onclick=logout;const[s,c]=await Promise.all([supabase.from('schools').select('id,name').order('name'),supabase.from('courses').select('id,title').eq('active',true).order('title')]);$('gaSchool').innerHTML=(s.data||[]).map(x=>`<option value="${x.id}">${esc(x.name)}</option>`).join('');$('gaCourse').innerHTML=(c.data||[]).map(x=>`<option value="${x.id}">${esc(x.title)}</option>`).join('');$('gaForm').onsubmit=async e=>{e.preventDefault();const due=$('gaDue').value?new Date($('gaDue').value+'T23:59:59').toISOString():null;const{data,error}=await supabase.rpc('assign_course_to_school',{p_school_id:$('gaSchool').value,p_course_id:$('gaCourse').value,p_due_at:due});$('gaMsg').textContent=error?error.message:`Назначено: ${data} учителям.`}}init();