import { supabase } from "./supabase.js"; import { lessons } from "./roqed-content.js"; import { requireRole,logout,$,esc } from "./utils.js";
let session,course,rows=[],done=new Set(),current=0;
const row=i=>rows[i],isDone=i=>row(i)&&done.has(row(i).id),unlocked=i=>i===0||isDone(i)||isDone(i-1);
async function init(){const a=await requireRole("teacher");if(!a)return;session=a.session;$("logout").onclick=logout;let r=await supabase.from("courses").select("id").eq("slug","roqed-science").single();if(r.error)throw r.error;course=r.data;let asg=await supabase.from("teacher_courses").select("id").eq("teacher_id",session.user.id).eq("course_id",course.id).maybeSingle();if(!asg.data){alert("Курс ROQED Science не назначен.");return location.replace("teacher.html")}r=await supabase.from("lessons").select("id,title,lesson_order").eq("course_id",course.id).eq("active",true).order("lesson_order");rows=r.data||[];r=await supabase.from("lesson_progress").select("lesson_id,status").eq("teacher_id",session.user.id);(r.data||[]).forEach(x=>x.status==="completed"&&done.add(x.lesson_id));let n=rows.findIndex(x=>!done.has(x.id));current=n>=0?n:lessons.length-1;render()}
function render(){let l=lessons[current];$("lessonModule").textContent=l.module;$("lessonTitle").textContent=l.title;$("lessonSubtitle").textContent=l.subtitle;$("lessonBody").innerHTML=l.html;$("prevLesson").disabled=current===0;$("nextLesson").disabled=current===lessons.length-1||!isDone(current);$("completeLesson").disabled=isDone(current);$("completeLesson").textContent=isDone(current)?"Занятие пройдено ✓":"Завершить занятие";$("lessonMenu").innerHTML=lessons.map((x,i)=>`<button class="lesson-link ${i===current?"active":isDone(i)?"done":unlocked(i)?"":"locked"}" data-i="${i}" ${unlocked(i)?"":"disabled"}>${isDone(i)?"✓ ":""}${i+1}. ${esc(x.title)}</button>`).join("");document.querySelectorAll(".lesson-link").forEach(b=>b.onclick=()=>{current=+b.dataset.i;render()});let pct=rows.length?Math.round(rows.filter(x=>done.has(x.id)).length/rows.length*100):0;$("courseProgress").style.width=pct+"%";$("courseProgressText").textContent=pct+"% завершено";$("finalTestBox").innerHTML=current===lessons.length-1&&isDone(current)?'<div class="card panel"><h2>Итоговый тест</h2><a class="btn" href="roqed-test.html">Перейти к тесту</a></div>':""}
$("prevLesson").onclick=()=>{if(current>0){current--;render()}};$("nextLesson").onclick=()=>{if(current<lessons.length-1&&isDone(current)){current++;render()}};
$("completeLesson").onclick=async()=>{let l=row(current);if(!l)return alert("Запустите SQL migration 002.");let now=new Date().toISOString();let r=await supabase.from("lesson_progress").upsert({teacher_id:session.user.id,lesson_id:l.id,status:"completed",started_at:now,completed_at:now,score:100,updated_at:now},{onConflict:"teacher_id,lesson_id"});if(r.error)return alert(r.error.message);done.add(l.id);await supabase.from("teacher_courses").update({status:"in_progress",started_at:now}).eq("teacher_id",session.user.id).eq("course_id",course.id).eq("status","assigned");render()};init().catch(e=>alert(e.message));