import{supabase}from"./supabase.js";import{requireRole,$,esc,fmtDate}from"./utils.js";async function init(){const auth=await requireRole("teacher");if(!auth)return;const number=new URLSearchParams(location.search).get("id");if(!number)return;$("certificate").innerHTML="<h2>Сертификат не найден</h2>";const{data:c,error}=await supabase.from("certificates").select("certificate_number,issue_date,final_score,teacher_id,profiles!certificates_teacher_id_fkey(full_name,schools(name,city)),courses(title)").eq("certificate_number",number).eq("teacher_id",auth.session.user.id).single();if(error)throw error;const verify=`${location.origin}${location.pathname.replace(/certificate\.html$/,"verify.html")}?id=${encodeURIComponent(c.certificate_number)}`,qr=`https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=${encodeURIComponent(verify)}`;$("certificate").innerHTML=`<div class="brand cert-brand"><div class="brand-mark">S</div> STEM <span>ACADEMIA</span></div><div class="cert-kicker">CERTIFICATE OF COMPLETION</div><h1>СЕРТИФИКАТ</h1><p class="muted">подтверждает, что</p><div class="cert-name">${esc(c.profiles?.full_name||"")}</div><p>успешно прошел(а) программу</p><h2>«${esc(c.courses?.title||"STEM курс")}»</h2><p>${esc(c.profiles?.schools?.name||"")} ${c.profiles?.schools?.city?"· "+esc(c.profiles.schools.city):""}</p><div class="cert-grid"><div><div class="muted small">Номер</div><b>${esc(c.certificate_number)}</b></div><div><div class="muted small">Дата</div><b>${fmtDate(c.issue_date)}</b></div><div><div class="muted small">Результат</div><b>${c.final_score??"—"}%</b></div><div><div class="muted small">Организация</div><b>STEM Academia</b></div></div><div class="cert-verify"><img src="${qr}" alt="QR проверки" onerror="this.style.display='none'"><div><b>Проверка сертификата</b><div class="small">${esc(c.certificate_number)}</div><a href="${verify}" target="_blank">${esc(verify)}</a></div></div>`}init().catch(e=>$("certificate").innerHTML=`<h2>Не удалось открыть сертификат</h2><p>${esc(e.message)}</p>`);